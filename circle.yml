general:
  branches:
    only:
      - master

machine:
  timezone:
    Asia/Tokyo
  python:
    version: 3.6.0

dependencies:
  pre:
    - sudo apt-get update
    - sudo apt-get install -y texlive texlive-lang-cjk latexmk
    - pip install -r requirements.txt

deployment:
  deploy:
    branch: master
    commands:
      - ./build_pdf.sh
      - git config --global user.email ffk2005@gmail.com
      - git config --global user.name tell-k
      - git checkout -b release
      - git add --all :/
      - git commit ./en.pdf ./ja.pdf -m "build pdf"
     # - git pull git@github.com:tell-k/resume.git release
      - git push git@github.com:tell-k/resume.git release

test:
  pre: 
    - echo "-----start test-----"
  override:    
    - echo "-----no test-----"
  post: 
    - echo "-----end test-----"
